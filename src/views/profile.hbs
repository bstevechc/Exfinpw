{{!-- llamamos a este archivo desde authentication.js --}}
<div class="container p-4 mt-5">
    <div class="row mt-5">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    {{!-- Para obtener el usuario vamos a nuestras variables globales en index.js --}}
                    <h3>Bienvenido {{user.nombres}} {{user.apellidos}}</h3>
                    <h4>{{user.email}}</h4>
                    
                </div>
            </div>
        </div>
    </div>
</div>